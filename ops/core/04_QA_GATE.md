# QA Gate

QA checks:

- does it satisfy the assignment?
- does it preserve product truth?
- does it break mobile-first usage?
- are actions visibly interactive?
- does it introduce hardcoded city logic where generic logic is required?
- does it fake dynamic data?
- are errors handled?
