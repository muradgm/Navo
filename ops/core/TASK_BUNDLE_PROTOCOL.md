# Task Bundle Protocol

A task bundle should contain:

```txt
ops/runtime/tasks/<TASK_ID>/
  packets/<TASK_ID>.md
  returns/<TASK_ID>.md
  qa/<TASK_ID>.md
  decisions/<TASK_ID>.md
```

Do not accept work without a packet and return.
