# Runtime

Runtime files track current execution state.

For Codex use:

1. Initialize agent using a boot file from `bootstrap/`.
2. Give it a task packet.
3. Store returns/reviews locally or in a task folder.

This package includes a starter `STATUS_SNAPSHOT.md` only.
