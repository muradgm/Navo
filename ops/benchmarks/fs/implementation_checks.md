# FS Implementation Checks

- `npm install` works from public registry.
- `npm run build` succeeds.
- No internal package registry lockfile is committed.
- State persists where expected.
- Components are not overloaded with city data.
